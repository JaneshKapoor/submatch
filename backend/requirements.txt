# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
websockets==12.0

# Configuration
python-dotenv==1.0.1
pydantic-settings==2.3.1

# YouTube download & subtitle fetch
yt-dlp==2024.5.27
# Cloud-safe subtitle fetching (bypasses YouTube bot detection on cloud IPs)
youtube-transcript-api==0.6.2

# Audio transcription — faster-whisper is 4-8x faster than openai-whisper on CPU
# Uses CTranslate2 backend with int8 quantization; no GPU required
faster-whisper==1.1.0

# Video/frame processing
opencv-python-headless==4.9.0.80
Pillow==10.3.0

# OCR (burned-in subtitle fallback)
pytesseract==0.3.13

# Text similarity
rapidfuzz==3.9.3

# Subtitle file parsing
webvtt-py==0.5.1

# HTTP client (for optional cloud APIs)
httpx==0.27.0

# Utilities
numpy==1.26.4
tqdm==4.66.4
